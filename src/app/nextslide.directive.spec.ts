import { NextslideDirective } from './nextslide.directive';

describe('NextslideDirective', () => {
  it('should create an instance', () => {
    // const directive = new NextslideDirective();
    // expect(directive).toBeTruthy();
  });
});
