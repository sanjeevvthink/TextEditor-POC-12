import { PrevSlideDirective } from './prev-slide.directive';

describe('PrevSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevSlideDirective();
    expect(directive).toBeTruthy();
  });
});
